function Room (game,roomData) {

    //this.game = game;

    console.log(roomData.length);
    console.log(roomData);

    /*Basic.apply(game,roomData[0])
    Basic.apply(game,roomData[1])
    Basic.apply(game,roomData[2])

    */
}